
import type { LucideProps } from 'lucide-react';
import { WalletCards } from 'lucide-react';

const WalletLogo = (props: LucideProps) => {
  return <WalletCards {...props} />;
};

export default WalletLogo;
